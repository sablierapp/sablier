package sabliercmd

// Regenerate the CLI reference documentation page from the cobra command
// definitions. Kept in sync via `go generate ./...` or `make cli-docs`.
//go:generate go run ../../cmd/docgen -config-src ../config -out ../../docs/content/reference/cli.md
