events {
    worker_connections 1024;
}

http {
    # Upstream for Sablier
    upstream sablier {
        server sablier:10000;
    }

    # Upstream for the whoami service
    upstream whoami {
        server whoami:8080;
    }

    server {
        listen 80;
        server_name localhost;

        # Dynamic strategy: Show loading page while container starts
        location /whoami {
            proxy_pass http://sablier/api/strategies/dynamic?session_duration=1m&names=whoami&show_details=true;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            
            # Required for dynamic strategy
            proxy_set_header X-Sablier-Upstream http://whoami;
        }

        # Blocking strategy: Wait for container to start
        location /blocking {
            proxy_pass http://sablier/api/strategies/blocking?session_duration=1m&names=whoami&timeout=60s;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            
            # Required for blocking strategy
            proxy_set_header X-Sablier-Upstream http://whoami;
        }

        # Health check endpoint
        location /health {
            proxy_pass http://sablier/health;
        }
    }
}
